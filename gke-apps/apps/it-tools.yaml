apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: it-tools
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://bjw-s-labs.github.io/helm-charts
    chart: app-template
    targetRevision: "4.*"
    helm:
      releaseName: it-tools
      values: |
        controllers:
          it-tools:
            containers:
              app:
                image:
                  repository: ghcr.io/corentinth/it-tools
                  tag: latest
                resources:
                  requests:
                    cpu: 50m
                    memory: 64Mi
        service:
          app:
            controller: it-tools
            ports:
              http:
                port: 80
        ingress:
          app:
            className: nginx
            annotations:
              cert-manager.io/cluster-issuer: letsencrypt-prod
              nginx.ingress.kubernetes.io/ssl-redirect: "true"
            hosts:
              - host: it-tools.natejsimonsen.com
                paths:
                  - path: /
                    service:
                      identifier: app
                      port: http
            tls:
              - secretName: it-tools-tls
                hosts:
                  - it-tools.natejsimonsen.com
  destination:
    server: https://kubernetes.default.svc
    namespace: it-tools
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
